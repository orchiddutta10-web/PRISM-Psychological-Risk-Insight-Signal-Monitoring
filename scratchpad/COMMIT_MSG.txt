fix(integration): post-merge cleanups — 84/84 API tests pass, end-to-end smoke verified

Fixes applied after the integration/ai-ml-iot merge to bring the API from
43 failing tests to 84 passing, with end-to-end smoke tests on:
- /api/v1/prism/predict → real 57-feature inference with classifier +
  regressor + data_sufficiency + feature_status + model_version
- /api/v1/medical/status → 21 docs loaded, 4 txt + 17 md
- /api/v1/medical/chat → synthesized answer with 5 evidence chunks +
  sources + confidence + crisis gate + medical disclaimer
- /api/v1/events/alerts → guardian-RBAC-gated alert retrieval

Code fixes:

- app/main.py: replace invalid walrus `:=(...)` walrus in audit middleware
  with a plain `now = datetime.now(timezone.utc)`; the previous form was
  Python 3.8+ syntax-correct but AST-rejected on some interpreters.
- app/main.py: drop the Colab ML readiness check (ColabMLService is no
  longer in the iot branch). Readiness probe now only checks DB.
- app/main.py: add audit-action classification for the new routes
  (typing insights, behavioral, trends, medical, prism).
- app/main.py: lifespan is now a no-op; ML engine wiring was iot's removal
  of PrismMLEngine, and the medical RAG prewarm moved to @app.on_event
  ("startup") on a daemon thread (matches iot's pattern).
- app/routes/companion.py: take iot's version (main's imports text_screening
  + nova_ai_service which iot removed).
- app/routes/physio.py: import List from typing for the VitalsOut list
  response_model.
- app/routes/voice.py: add `import os` and `import shutil` (used by raw
  audio persistence); resolve missing `retention_consent` by querying
  ConsentGrant for `voice_retention` modality (the new privacy spec
  requires explicit consent for raw audio storage; default is discarded).

Test cleanups:

- conftest.py: set RATE_LIMIT_ENABLED=false at import time so the
  per-IP sliding-window rate limiter doesn't block tests when they're
  fired rapidly against TestClient.
- test_api.py: test_pulse_isd_trigger now seeds a ConsentGrant(pulse) row
  before the first ingest call (required by the endpoint's consent gate).
- test_api.py: test_voice_checkin's `audio_discarded is False` assertion
  updated to `True` to match the new privacy-by-default behavior (raw
  audio is always discarded unless voice_retention consent is granted).
- test_medical.py: greet test now asserts "PRISM Health Coach" instead of
  "medical assistant" (the greeting text was reworded on iot).

Removed from main (orphaned after the iot merge replaced them):

- app/demo_simulation_engine.py
- app/tests/test_demo_auth.py, test_drift_monitor.py, test_feature_utils.py,
  test_model_regression.py, test_nova.py, test_offline.py,
  test_phase10_ml.py, test_production_feature_builder.py,
  test_sensor_routes.py, test_xai_engine.py

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>